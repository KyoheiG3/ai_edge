# generated by octocov init
coverage:
  if: true
  paths:
    - packages/ai_edge/coverage/lcov.info
    - packages/ai_edge_fc/coverage/lcov.info
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
